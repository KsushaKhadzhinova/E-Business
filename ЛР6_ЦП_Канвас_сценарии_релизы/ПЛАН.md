# План выполнения ЛР6 (NotaCode)

ЛР6 = 5 методик цепочкой: 6.1 ЦП и цифровой товар → 6.2 Канвас → 6.3 бизнес-логика и сценарии → 6.4 страницы и экраны → 6.5 релизы. Общая оценка времени: ≈ 14–16 ч (из них ≈ 4 ч — досъём данных самой).

## 0. Порядок чтения (≈ 1,5 ч)

1. `ЗАДАНИЕ.md` целиком (разделы 3–7 — по одной методике, раздел 9 — чек-лист сдачи).
2. Методики в `Материалы\` в порядке 6.1 → 6.5; в каждой читать: пошаговый порядок, формулы индексов, итоговые формы, контрольный лист.
3. В каждом шаблоне xlsx — лист «Инструкция» и строки «Пример…» (их заменяем своими).
4. Входы проекта: `E:\NotaCode\docs\business\01-project-concept.md` (§1 проблемы, §4 сегменты, §6 тарифы), `E:\NotaCode\docs\planning\agile-plan.md` (§7 эпики, §8 MoSCoW и волны нотаций, §9 спринты, MVP S10), `E:\NotaCode\docs\architecture\README.md` (§5 сценарии, §8 Web IDE), отчёты ЛР1–ЛР5 (архив `_архив_DiagramCode\labs\`).

## 1. Доказательная база (≈ 4 ч, делать самой)

1. Отзывы (≥ 30): G2 (g2.com → поиск «draw.io», «Lucidchart», «PlantUML») → фильтр по оценке 1–3 звезды → выписать 10 отзывов о ручной раскладке/отсутствии проверки правил; Capterra — то же (10); Reddit r/softwarearchitecture, r/PlantUML, Habr (поиск «IDEF0 программа», «DFD диаграмма инструмент») — 10 высказываний. На каждый — дата, площадка, ссылка, цитата ≤ 1 предложения → лист «Исходные данные» (строки SRC-015…, достоверность 3–5).
2. Опрос студентов (≥ 20): Google Forms, 6 вопросов — «сколько часов на диаграммы одной ЛР», «получали ли замечания по нотации», «чем рисуете», «готовы ли платить 4 USD/мес за проверку правил и экспорт», «какие нотации нужны», «оставьте email для бета-теста». Ссылка — в чат группы/потока. Итоги → SRC-016, проверка H-01, H-02.
3. (опционально) Интервью с 1–3 преподавателями (S-04): как проверяют IDEF0/DFD, нужен ли набор шаблонов заданий → M-003, PR-003.

## 2. Методика 6.1 (≈ 1,5 ч)

1. Скопировать `Шаблон_формулирования_ценностного_предложения_и_цифрового_товара.xlsx` → `VBA\data\` → открыть → Alt+F11 → Import `VBA\LR6_1_CP_Tovar.bas` → Alt+F8 → `Main`.
2. Проверить листы: «Проблемы клиентов» (P-001 = 90, «Критический»), «Проверка связности» (93/84/75), «Дашборд».
3. Заменить строки SRC-015/016 реальными отзывами и опросом; при изменении оценок — поправить `Python\lr6_d1_cp.py` и перезапустить `python -B lr6_calc.py` (чтобы отчёт и Excel совпадали).
4. Сверить ИПП (формула методики) — таблица 1 отчёта; ИКЦП — таблица 3, ИКСТ — раздел 2.2.5.

## 3. Методика 6.2 (≈ 1 ч)

1. Шаблон Канваса → импорт `LR6_2_Canvas.bas` → `Main`.
2. Проверить 02_Канвас (статусы J3:J11), 11_Дашборд (полнота 71 %, достоверность 76 %), 10_Паспорт (все «Заполнено»).
3. Индексы методики (доказательность, полнота 88,9 %, связность 80 %) — рисунок 3 и таблица 6 отчёта.

## 4. Методика 6.3 (≈ 1 ч)

1. Шаблон бизнес-логики → импорт `LR6_3_Scenarii.bas` → `Main` (исправляет двойную проверку данных на Q4:Q203).
2. Проверить «Связность» (10 из 10 «Пройдено»), «Дашборд» (MVP-требований 7).
3. Паспорта сценариев S-003, S-004, S-007, S-008 — приложение А отчёта.

## 5. Методика 6.4 (≈ 1 ч)

1. Шаблон страниц и экранов → импорт `LR6_4_Ekrany.bas` → `Main`.
2. Проверить «Проверка_связности» (все «Норма»), «Приоритизация» (8 единиц P1), «Дашборд» (интегральная готовность 1,0).
3. Мокапы: `python -B Python\lr6_mockups.py` (рендер через Edge) — уже выполнено, 17 PNG.

## 6. Методика 6.5 (≈ 1 ч)

1. Шаблон релизов → импорт `LR6_5_Relizy.bas` → `Main` (добавляет защиту пустых строк в Q/R реестра).
2. Проверить «Оценка_релизов», «Дашборд» (38 единиц, 16 рекомендовано шаблоном в R1).
3. Сверить с agile-plan: R1 = MVP S10 (06.12.2026) — 16 единиц по расчёту шаблона + правовые документы; RU-005, RU-009, RU-014 (Must в плане) по шаблону в R2 — согласовать как PO.

## 7. Рисунки и отчёт (≈ 1,5 ч)

1. `cd Python` → `python -B lr6_calc.py` → `python -B lr6_charts.py` → (опц.) `python -B lr6_fill_excel.py`.
2. Снять скриншоты дашбордов пяти книг (см. `ИНСТРУКЦИЯ_ДЛЯ_МЕНЯ.md`).
3. Проверить `ОТЧЕТ.md`, заменить все «🔲 ДОСНЯТЬ», собрать docx сборщиком `_tools\build_lab_docx.py`.

## Артефакты

`ОТЧЕТ.md`, 5 заполненных книг (`Excel_заполненные\`), `VBA\*.bas` + `VBA\data\*.tsv`, `Python\*.py`, `Рисунки\*.png` (15 в отчёте + мокапы), `ЧЕКЛИСТ.md`.
